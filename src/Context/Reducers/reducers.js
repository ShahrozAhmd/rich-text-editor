import { state } from "Context/State/state";

const Reducer = (state = state, action) => {
  switch (
    action.type
    // case:

    //     break;

    // default:
    //     break;
  ) {
  }
};

export default Reducer;
