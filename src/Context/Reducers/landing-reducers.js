import { state } from "Context/State/state";

const {landing} = state;

const Reducer = (state = landing, action) => {
  switch (
    action.type
    // case:

    //     break;

    // default:
    //     break;
  ) {
  }
};

export default Reducer;
