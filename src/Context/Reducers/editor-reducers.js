import { state } from "Context/State/state";

const {editor} = state;

const Reducer = (state = editor, action) => {
  switch (
    action.type
    // case:

    //     break;

    // default:
    //     break;
  ) {
  }
};

export default Reducer;
